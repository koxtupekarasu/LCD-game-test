# LCDゲーム仮 正本ドキュメント：現行実装仕様

最終整理: 2026-03-26

この文書は、現時点で**実装済み・現行として扱う仕様のみ**をまとめた正本である。旧案・保留・要調整は別冊の「未実装・保留メモ」を参照。

---

## 1. 全体方針
- 作品の基本形は**携帯育成液晶ゲーム風**。
- 描画は **Canvas方式** を基本とし、一部情報表示は **DOM overlay** を使う。
- 画面内の用語や設計は、単なるRPGではなく**観測・管理・干渉**を伴う育成機として整理する。
- ただしUI表層の見え方は、まず**育成玩具として一目で分かること**を優先する。
- 内部論理解像度は **384 × 288**。
- モンスター表現の基本単位は **16×16 / 1bitドット**。
- HiDPI対応あり。CSS表示サイズは維持しつつ、canvas実解像度は devicePixelRatio で高解像度化する。

---

## 2. 基礎アプリ構成
- 基本ファイル:
  - `index.html`
  - `app.js`
  - `style.css`
  - `manifest.webmanifest`
  - `service-worker.js`
- 静的配信で動作確認済み。
- 実時間差分ベースの更新を採用しており、固定 `1/60` 更新は廃止済み。
- `performance.now()` ベースの `dtSec` を使い、`0〜0.1` にクランプする。

---

## 3. 世界内の基本存在（実装前提の用語）
- 媒体の正式名称: **AD（Artifact Disk / アーティファクト・ディスク）**
- 個体群の正式英名: **Artifact Domain**
- 学術和名: **AD由来疑似生命体領域群**
- ゲーム内通称: **アド**
- ゲーム内では、個体群も単体個体も基本的に「アド」または個体名で呼ぶ。
- `AD` は媒体そのもの、`アド` はそこから発生・観測される個体群/個体を指す。

---

## 4. モンスター / 個体データ構造（現行）
- Monsterデータの正本は `monster`。
- `state.monSprite` などの旧二重管理は廃止済み。
- 代表構造:
  - `id`
  - `age`
  - `weight`
  - `stage`
  - `spriteSet`
    - `base`
    - `faces`
    - `actions`
    - `icons`
  - `stats`
    - `maxHp`
    - `staminaMax`
    - （旧 `attack / defence / speed` は現行主軸から外れており、今後削除対象）
  - `personality`
    - `aggression`
    - `curiosity`
    - `calmness`
  - `runtimeState`
    - `hp`
    - `stamina`
    - `mode`
    - `faceId`
    - `actionId`
    - `actionFrame`
    - `actionTimer`
- 初期化 / 正規化 / 検証:
  - `createDefaultMonster`
  - `normalizeMonster`
  - `validateMonster`
- Save format は version 付き:
  - `{ version, monster }`
- 旧形式セーブからの自動移行あり。
- JSON破損時のフォールバック初期化あり。

---

## 5. ドットエディタ・スプライト運用（現行）
### 5.1 ドットエディタ
- 16×16 / 1bit 前提のドットエディタ基盤あり。
- Base差分を編集可能。
- 差分管理UI:
  - 右ペイン管理
  - 横3列 × 縦4列
  - 1ページ12枠
  - カテゴリ: `FACE / ACTION / ICON`
- 差分は**固定配置方式**。
- 差分データはすべて**フル16×16保持**。
- 透明色なし。
- 新規差分作成時は Base を自動コピーして開始。
- サムネは本体16×16をそのまま等倍表示。
- 実装済み機能:
  - Undo / Redo
  - Cボタン導入
  - 矩形選択
  - Copy / Cut / Paste
  - 水平 / 垂直反転
  - 1ドットシフト

### 5.2 スプライト規格・アセット運用
- 基本方針は **1アイコン = 1PNG**。
- 基本サイズ:
  - food icon : 16x16
  - ui icon   : 16x16
  - fx icon   : 16x16
  - ad sprite : 16x16（将来32x32拡張可能）
- アセットカテゴリ:
  - `assets/food`
  - `assets/ad`
  - `assets/ui`
  - `assets/fx`
- 食品アイコンは現時点で以下を実装済み。
  - `小さい肉`
  - `簡易栄養ドリンク`
- 同系統アイテムは、ベースシルエットを揃えつつ、UI側で等級表示を重ねる設計を採用する。

---

## 6. 基本育成・日常要素（現行）
### 6.1 時刻・睡眠
- ゲーム内時計は**現実時間と同速**で進行。
- 起動時は現実時刻に同期。
- 夜は自動睡眠状態に入る。
- 睡眠中は `Zzz` 表示と暗幕演出あり。
- `chronotype`（朝型 / 昼型 / 夜型）あり。
- `SLEEP` は**睡眠中のみ実行可能**。
- `SLEEP` 実行で `isTuckedIn = true`。
- 自然睡眠のみの場合は効果半減、寝かしつけ済みなら効果フル。

### 6.2 うんち / hygiene
- うんちは一定時間で自然発生。
- 最大3個。
- 放置で `hygiene` 低下。
- `WC` で全消去。
- 出現時にポン演出あり。
- 出現位置はモンスター左寄りへ調整済み。

### 6.3 FOOD / SLEEP / HEAL の内部処理
- `applyFood()` / `applySleep()` / `applyHeal()` 実装済み（旧 `applyFeed()` は FOOD 方針へ移行）。
- 内部主要値:
  - `hp`
  - `stamina`
  - `hunger`
  - `stability`
  - `damage`
  - `weight`
- 用語整理:
  - `mood` は現行採用しない
  - 安定度 = `stability`
  - `hunger` は内部変数名、表示上は **充足値**
- `hunger` は満腹度方式:
  - FOODで増加
  - SLEEPで減少
- FOODとHEALの役割分担:
  - FOOD内の修復系 = 軽い損傷回復や日常メンテ補助
  - HEAL = 明確な不調・深い損傷・異常状態の治療
- ログ差分は実更新値ベースで計算。
- 差分ゼロは `+0 / -0` を表示しない。
- 差分なし時は `_nochange` テンプレへ分岐する。

### 6.4 FOODメニュー（現行）
- メニュー名は **`FOOD`** を採用し、旧 `FEED` 表記は内部コード上も `FOOD` に統一済み。
- FOODは即時実行式ではなく、**所持フード一覧から1つ選んで使用**する方式。
- 画面構成は**上部選択ペイン + 下部詳細ペイン**の2段構成。
- 上部ペインは **56 / 44** 比率で、左に一覧、右にアイコンペインを置く。
- 初期開放フード:
  - `小さい肉`
  - `簡易栄養ドリンク`
- 上記2種は**無限在庫**。
- 在庫表示は `∞` を使い、`x` は表示しない。
- 右ペインは**アイコンのみ**を表示し、タイトルやフード名は出さない。
- FOODアイコンは `assets/food/meat_small.png` と `assets/food/drink_simple.png` を接続済み。
- FOOD使用後は結果表示を行い、**メインメニューへ戻らずFOOD選択画面へ戻る**。
- FOODリザルト遷移には TRN 系と同系統のフェード演出を使う。

### 6.5 HEALメニュー（現行仮実装）
- HEALは**仮実装として** `PATCH / STABILIZE / PURGE` の3メニュー構成。
- 画面構成は、左に波形、中央にAD+スロット、右に見込み、下に短文説明の4ブロック。
- 上段比率は **28 / 40 / 32**。
- 左右構成は、左をやや縮小、中央維持、右を拡張した最終比率を採用。
- 中央ADは `assets/ad/ad_media_base.png`、中央スロットは `assets/ad/ad_slot_base_v1.png` を使用。
- 中央ADは異常状態が `0` の時でもごく軽く上下する。
- 各メニューの異常状態表現:
  - `PATCH`: AD自体に大きめの欠損表現。改善で欠損が減る。
  - `STABILIZE`: ブレ + グリッチ表現。改善で収束。背面ラインは削除済み。
  - `PURGE`: 汚染 / ノイズ表現。改善で汚れが減る。汚染エフェクトは中央ペイン内に収まるよう修正済み。背面ラインは削除済み。
- 実行演出:
  - ADがスロットへ差し込まれる。
  - 差し込み時間は `220ms`。
  - 刺さった後に `120ms` 停止してから戻る。
  - フラッシュ時間は `280ms`。
  - 現行演出は背面の下地フラッシュ + 輪郭発光 + 前面白フラッシュ。
  - 十字スパーク案は削除済み。
- 差し込み中はADの上下揺れ / 横揺れを止めるが、欠損 / 汚染 / グリッチ表現は維持する。
- 右ペインは見込みゲージを大型化し、`現在値 -> 見込み値` を表示する。
- 見込み部分はゆっくり点滅する。
- `STA` も見込みゲージ表示し、コスト消費と分かる専用パターンを使う。
- 実行中は右ペインの見込み数値 / ゲージを途中で動かさず、結果はリザルトで反映する。
- `NOISE / DESYNC / CONTAMINATION` は `Lv3` から同一サイクル内で `0` まで落ちず、下限 `Lv1` を持つ。
- `DECAY` は `Lv3` から同一サイクル下限 `Lv2` を持つ。
- `damage` も重症時には同一サイクル下限あり。
- HEAL使用後は結果表示を行い、**メインメニューへ戻らずHEAL選択画面へ戻る**。
- HEALリザルト遷移には TRN 系と同系統のフェード演出を使う。

---

## 7. UI文字描画・メニュー（現行）
### 7.1 UI文字描画
- UI文字描画は `uiText.js` に集約済み。
- ビットマップフォント導入済み。
- 主要構成:
  - `bitmapFontSpec.js`
  - `bitmapFontData.js`
  - `bitmapTextRenderer.js`
  - `uiText.js`

### 7.2 カーソル・メニュー
- カーソル表現:
  - `▶` を使うA案を採用
  - 点滅あり
  - 移動時フラッシュあり
  - `performance.now()` ベース
- カーソル位置は**文字基準**。
- 上段メニューには左安全マージン方式を導入済み。
- メニュー枠線は上下とも削除済み。
- 線構成:
  - 上段メニュー: 下線
  - 下段メニュー: 上線
- ベースラインはフォント実寸ベースで算出。
- 下段メニュー位置は +12px 調整済み。
- ラベル群（現行採用）:
  - `STAT / FOOD / TRN / BTTL / ADV / WC / SLEEP / HEAL / EDIT`

### 7.3 UI言語共通ルール
- **予告は記号、結果は数字** を共通ルールとする。
- 増減記号の意味:
  - `+` = 小上昇
  - `++` = 中上昇
  - `+++` = 大上昇
  - `-` = 小減少
  - `--` = 中減少
  - `---` = 大減少
- 実結果は数値で表示する。
  - 例: `+2 / -1 / +0.5`
- 等級は名前ではなくUI補助情報として扱う。
- アイコン付きオブジェクトの等級表示は `I / II / III` を**右下寄りに文字描画**する。
- rank内部値:
  - `0` = なし
  - `1` = I
  - `2` = II
  - `3` = III
- 増減記号ヘルパは共通化済みで、`mid threshold = 2` / `large threshold = 3` を採用する。
- 汎用ランク表示 `I / II / III` は共通レイヤー化済みで、FOODアイコンから他カテゴリへ流用可能。
- 詳細なUI言語仕様は `DOTMON_UI言語_完全仕様.md` を参照する。

### 7.4 オーバーレイ画面と共通ヘッダー
- `STAT / FOOD / HEAL` はオーバーレイ画面として扱う。
- 表示時は直前画面のスナップショットを背面ゴーストとして薄く表示する。
- 背面ゴーストは `opacity 0.08` / `blur 1.2px` / `scale 1.01`。
- ゴースト表示ではヘッダー帯をマスクして非表示にする。
- オーバーレイ画面全体にはメイン画面と同系統の走査線を掛ける。
- 走査線は `1px線 / 3px間隔 / alpha 0.03` 系。
- 共通ヘッダーは左上にメニュー名、右上に時計を表示する。
- ヘッダー名の現行対応:
  - `status` -> `STATUS`
  - `food` -> `FOOD`
  - `heal` -> `HEAL`
  - `trnmode / trn / trnlog` -> `TRAINING`
  - `bttllog` -> `BATTLE`
- `BTTL` 本編のみはヘッダー非表示とし、`BATTLE` 表示を無理に載せない。
- `TRN` のヘッダー帯サイズは他メニューと同等へ調整済み。
- `TRN / BTTL` の暗幕フィルタは削除済みで、走査線のみ維持する。

### 7.5 デバッグメニュー
- `ESC` で開閉するデバッグメニューを実装済み。
- 表示中は本編入力を停止する。
- 構成は左カテゴリ / 右詳細 / 下説明。
- 現行カテゴリは `STATUS / ABNORMAL / FLAGS / TIME / SCREEN` 系。
- 下部文言は `HP/STB/STA/DMG を調整`、`プリセットを適用` に修正済み。

---

## 8. ログ・文言システム（現行）
- 日本語ログは **DOM overlay** で表示。
- フォントは `PixelMplus12` を使用。
- overlay表示中は scanline 停止。
- `i18n.js` 導入済み。
- `DotmonI18n.t(key, params)` による辞書管理あり。
- ログは `state.logStyle = "normal" | "corrupted"` に対応する構造あり。
- `corrupted` 未定義時は `normal` にフォールバック。
- ログ文体方針:
  - 主語なし
  - 短文
  - 記録・観測寄り
- FOOD / SLEEP / HEAL などのログテンプレは style + action ベースで解決。

---

## 9. STAT画面（現行）
### 9.1 構成
- 現在は **4ページ構成**。
- Page1 / Page2 / Page3 / Page4(SKILL) を切り替え可能。
- 各ページに選択カーソルあり。
- 下部説明ペインあり。
- 選択中項目の意味を下ペインへ表示。

### 9.2 Page1
- 数値・現在値確認ページ。
- バー + 数値中心。
- 主要状態値を表示。
- 体重 `weight` 表示対象に含める方針を採用。

### 9.3 Page2
- 傾向確認ページ。
- レーダーチャートではなく**バー / 一覧型表示**を現行採用寄りとする。
- personality・傾向値・評価系の表示に使用。

### 9.4 Page3
- 成長傾向 / 進化予兆 / 観測メモ寄りの読ませるページ。
- 現在は土台と仮文言を含む構成。

### 9.5 Page4（SKILL）
- 上部に `FINISH SKILL` と `SET 1-3` を表示する。
- 固有必殺は確認枠、共有スキル3枠は**セット順が優先度**となる。
- 下部には **10列 x 4行** のグリッドを置く。
- 行ラベルは `SHORT / MID / LONG / SUPPORT`。
- グリッドはスキル名を出さず、**セルのみ**で表示する。
- `SET` 選択時にグリッドへ移動し、グリッド内はループ移動する。
- グリッド内では `B` で直前の `SET` 選択へ戻る。
- 「共有スキル0個になる外し方」も警告を出したうえで許可する。
- 警告表示時でも下部説明ペインの高さは固定する。
- 上段表示から `必殺 / 攻撃系 / 補助系` の右側文言は削除済み。

### 9.6 旧Page1/2仕様との関係
- 以前の `STAT 1/2 / 2/2` ベースは、現在の3ページ土台 + SKILLページへ発展的に統合された扱いとする。
- 以前のPage2で使っていた「直近変化」保持・説明文構造は、現行構成へ継承されている。

---

## 10. TRAIN（現行）
### 10.1 位置づけ
- TRNは**信号品質を整える / 上げる主成長手段**。
- 戦闘の「攻勢」軸を日常行動で整える意味を持つ。

### 10.2 基本構造
- 左: 円周波形ミニゲーム
- 右: AD表示 + 右下ミニ枠
- Aで停止判定
- Bで戻る / 退出
- Cは無効

### 10.3 モード
- `CALIBRATION`
- `BOOST SYNC`
- `NOISE CUT`

### 10.4 判定
- `BAD / NEAR / OK / SUCCESS`
- 内部成功率（安定度・充足値・スタミナ依存）とプレイヤー入力の二層構造。

### 10.5 波形・成功帯
- 高速ループ型の円周波形。
- 進行は easeInOutSine 型。
- 成功帯はセッション開始時ランダム固定。
- 成功帯は内側寄せ済み。
- 成功帯 / クリ帯突入フィードバックあり。

### 10.6 演出
- BAD時は画面全体横ブレ + 残像。
- 右ペイン瞬間フィードバックあり。
- 判定行は擬似太字化。
- トレーニング終了後は即ログ遷移せず、Aで次セッション / BでRESULTへ。
- 旧フリーズ要因だった `BTTL_SPRITE_SCALE` 未定義参照は修正済み。

### 10.7 RESULT
- 実行回数
- 信号品質 / スタミナ / 安定度の総和
- 総括フレーバー1行

### 10.8 スタミナ制御
- スタミナ0でトレーニング不可。
- 開始不可時は画面全体ブレ。
- スタミナ0到達時は強制RESULT。

### 10.9 成長保存
- トレーニング由来の成長処理・履歴保存は導入済み。
- 主成長対象:
  - `signalQuality`
  - `stability`
  - `adIntegrity`
  - `personality.*`
- 補助対象:
  - `maxHp`
  - `staminaMax`
  - `weight`
- 履歴系:
  - `trainingCount`
  - `trainingTypeCounts` または同等構造

### 10.10 効果予告表現
- TRN右下ペインの効果予告は**記号表示**を使う。
- 例:
  - `SIG ++`
  - `STA --`
- これはTRN専用ではなく、DOTMON全体の増減予告記号ルールに従う。

---

## 11. BTTL（現行）
### 11.1 全体
- **リアルタイムオートバトル**。
- 旧ターン制は撤廃済み。
- Bで戦闘中断し `ABORT` としてRESULTへ遷移可能。
- 戦闘開始時フリーズは修正済み。

### 11.2 画面構成
- 3ペイン構成:
  - 左: メイン戦闘
  - 右: `SIGNAL MENU` + `OBS LOG`
  - 下: 日本語ログ（意味づけ層）
- 右は事実 / 端末ログ、下は意味 / 観測記録の二層。
- BTTL本編はヘッダー非表示で、戦闘画面サイズを優先する。

### 11.3 レンジ
- 相対位置ベースで `SHORT / MID / LONG` を判定。
- 中央レンジバー上に敵味方マーカーを表示。
- `RANGE SHORT/MID/LONG` を OBS LOG に出す。
- SHORT攻撃後は反動で距離が戻る。

### 11.4 攻撃表現
- MID/LONG: 飛翔体
- SHORT: 近接寄り斬撃エフェクト（ゲート直発生）
- 着弾判定ゲート表示あり

### 11.5 HEAVY / リアクション
- `C:REACT`
- 判定:
  - BAD = 被弾
  - NEAR = DODGE
  - OK = PARRY
  - SUCCESS = REFLECT
- 反応窓中は減光 / 疑似ズーム / スロー演出あり。

### 11.6 BREAK
- 敵味方とも BREAK 値あり。
- BREAKは第2HPではなく崩れ蓄積。
- BREAKゲージ表示あり。
- 高蓄積強調あり。
- HEAVY / PARRY / REFLECT / SHORT圧などで増加。
- 自然回復あり。
- 敵BREAKゲージは下方向へ調整済み。
- 敵スプライトはやや上方向へ調整済み。
- 敵側ドライブバッジ上げ / 自側シグナルバッジ下げ方向で位置調整済み。

### 11.7 HP / STA / BREAK
- HP: 生存値
- STA: SIGNALコスト用
- BREAK: 崩れ蓄積

### 11.8 SIGNAL
- `SIGNAL MENU` から方針介入を行う。
- 現行分類:
  - `BOOST`
  - `STABILIZE`
  - `CALIBRATE`
  - `OVER CLOCK`
- SIGNALはプレイヤー側専用で、敵は同一メニューを使わない。

### 11.9 DRIVE
- 敵専用の内部駆動カテゴリとして **`DRIVE`** を採用。
- 現行分類:
  - `SURGE`
  - `GUARD`
  - `FOCUS`
  - `RAMPAGE`
- DRIVEは敵が自律的に発動する内部状態として扱う。
- プレイヤー側に対しては、敵側面などへ発動文言をそのまま表示して認識可能にする。

### 11.10 スキル制
- バトルの基盤レンジは `SHORT / MID / LONG` を維持する。
- 攻撃はレンジ抽象攻撃ではなく、**スキルベース**で行う。
- 現在レンジに対応した攻撃スキルが無い場合、そのレンジでは攻撃できない。
- ただし、以下の基礎行動は可能。
  - 接近
  - 離脱
  - 防御
- 攻撃不能時の基礎行動優先は固定ではなく、SIGNALやAI傾向で変動してよい。

### 11.11 スキル構造
- 各アドは固有スキルを **1つ** 持つ。
- 固有スキルは**必殺技枠**として扱う。
- 各アドは共有スキルを複数習得可能。
- 習得数上限は設けない。
- ただし、戦闘中に持ち込める共有スキルは **3つまで**。
- 共有スキルカテゴリは
  - 攻撃系
  - 補助系
  とする。

### 11.12 必殺技
- 固有必殺スキルは、常時使用可能ではなく**必殺技ゲージ**で解禁される。
- ゲージ蓄積は**基本、時間経過**。
- 満了時はゲージ自体が強めに点滅する。
- ゲージ上の余計な文字表示と常時 `FINISH OK` 文字表示は削除済み。
- `FINISH OK` はバッジ表示に周期表示する。
- 一戦中の使用回数は基本1回。
- 使用後は再蓄積しない。
- 発動は**プレイヤー任意**。
- 入力は**専用ボタン短押し**。
- 発動時に SIGNAL と同系統のミニゲームを実行し、結果で効果増減。
- 固有必殺にもレンジ制限を持たせる。
- 必殺ゲージ満了時は、AIが対応レンジへ寄る挙動を持つ。
- `FIN SKILL / FIN RESULT` はスキル名を出さない固定ラベルとする。
- `FIN RESULT` の結果表示は右側サークル中央へ出す。

---

## 12. UI言語・関連文書（現行）
- UI言語の正本は `DOTMON_UI言語_完全仕様.md`。
- スプライト規格の正本は `DOTMONスプライト規格.md`。
- 現行実装は、この2文書を参照しながら個別画面へ反映していく方針とする。

---

## 13. 実装メモ上の位置づけ
- 本文書には**現行実装と現行採用仕様のみ**を記載する。
- 未実装・保留・調整待ちは `LCDゲーム_正本_未実装保留.md` へ移す。
- 世界観と意味づけは `LCDゲーム_正本_世界観設定.md` を参照する。


---

## 13. ADV（現行仮実装）
### 13.1 基本位置づけ
- `ADV` は現状、**ノードベース探索の土台**まで実装済みの仮実装段階とする。
- 完成系ではなく、**マップ選択 / ノード管理 / 名称確定 / 報酬傾向**の骨組み確認を目的とした段階である。
- ここで探索しているのは、実施設そのものではなく、**実施設区画に対応する観測・管理・再構成領域**である。
- ADV文言は「観測接続」「再構成領域」寄りへ調整済み。

### 13.2 ノード管理
- ADVは**ノード単位**で管理する。
- 現行ノード定義では、最低限以下を保持できる。
  - `layer`
  - `radial`
  - `stateLabels`
  - `localMapId`
  - `visibleFromStart`
  - `unlocked`
  - `specialResponseToAd`
- **全体断面図のノード1つ = 局所図1枚**を前提とする。

### 13.3 現行ノード構成
- 現在の可視ノードは合計5つ。
- 初期探索可能ノード:
  - 保守区画
  - 記録区画
  - 回線区画
- 初期可視・未解放ノード:
  - 制御区画
  - 生態区画

### 13.4 断面図表示（現行仮実装）
- 右ペイン断面図はノード定義参照へ切り替え済み。
- 初期段階では、区画名は `???` を基本とする。
- 制御区画 / 生態区画は**見えているが未解放**として表示する。
- 状態ラベル差分は軽い表示のみ仮実装済み。
- 現在の右ペインビジュアルは**最終確定ではない**。
- 円形施設案は採用せず、**巨大主軸塔ベースの縦長断面イメージ**が現行方針である。

### 13.5 名称確定（現行仮実装）
- `advState` は保存対象に含める。
- 現在は、**初回の有効な「調べる」**を通した時点で管理照合ログが入り、`??? -> 正式名` が確定する。
- 確定後の名称は保存をまたいで保持される。
- これは簡易実装であり、将来的には
  - 管理ログ
  - 作業ログ
  - アーカイブ
  - 端末照合
  の混合トリガーへ拡張する前提。

### 13.6 報酬傾向（現行仮実装）
- 区画ごとに主報酬傾向を分離済み。
- 現行傾向:
  - 保守区画：`HEAL / MATERIAL` 寄り
  - 記録区画：`ARCHIVE` 寄り
  - 回線区画：`SIGNAL` 寄り
- `汚染 / 異常 / 危険` の状態ラベルにより、副傾向差分を持てる。
- 制御区画 / 生態区画の本運用は未解放段階のため、現行実プレイ対象外。

### 13.7 探索画面（現行仮実装）
- 探索画面は一度、ワイヤーフレーム基盤 + 面塗り強化まで試作実装された。
- ただしこの方式は**現行完成仕様ではない**。
- 「移動感」は出せるが、「観測 / 解読」の主軸に対して最適かは再検討中。
- 現時点では、**地点探索 + 観測 / 解読型へ再設計を検討している段階**とする。

---

## 14. ADVに関する運用上の現行整理
- 現行で「実装済み」とみなすのは、**ノード管理・断面図選択・名称確定保存・区画別報酬傾向**までである。
- 探索画面の表示方式は、まだ**仮実装 / 再検討中**であり、完成仕様として固定しない。
- 旧来の「歩行迷宮型」をADV全体の主方式とする方針は、現行採用仕様には含めない。
